spack:
  include:
  # Include configuration from this repositories .github/build-ci/includes directory, environment variable defined in build-ci
  - path: $SPACK_MANIFEST_REPOSITORY_PATH/.github/build-ci/include
  specs:
  - 'access-mom6@git.{{ ref }}=stable +access3 ~mom6_solo ^openmpi'
  view: false
